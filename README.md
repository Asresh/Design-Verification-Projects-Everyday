# Design Verification Projects — Everyday

A daily series of self-contained design-verification projects in SystemVerilog/UVM. One documented, simulate-able verification exercise per day.

Each day lives in its own folder (`DayN/`) containing a self-contained project with source, a self-checking testbench, a Makefile, a waveform image, and a README write-up.

## Index

| Day | Project | Key concepts | Folder |
|-----|---------|--------------|--------|
| 1 | Constrained-Random FIFO Scoreboard | Reference-model scoreboard, `$`-queue golden model, directed + constrained-random stimulus, functional coverage, SVA assertions | [Day1/](Day1/) |
| 2 | UVM APB4 Register-File Verification | Full UVM agent (driver/monitor/sequencer), golden reference-model scoreboard, layered sequences, virtual sequencer + virtual sequences, functional coverage, APB protocol SVA | [Day2/](Day2/) |
| 3 | UVM AXI4-Lite Register-Block Verification | Five-channel AXI4-Lite agent (driver/monitor/sequencer), golden reference-model scoreboard, layered + virtual sequences, functional coverage, AXI4-Lite protocol SVA (VALID/READY stability, response codes) | [Day3/](Day3/) |
| 4 | UVM UART Verification Environment | TX + RX agents, serial-line monitors that reconstruct bytes, dual sent-vs-serialized / on-wire-vs-received scoreboard, directed + random sequences across baud settings, data×baud coverage, start/stop framing SVA | [Day4/](Day4/) |
| 5 | UVM RAL (Register Abstraction Layer) Demo | `uvm_reg` model with RW/RO/W1C fields, `uvm_reg_adapter` + explicit `uvm_reg_predictor`, front-door and back-door access, built-in `uvm_reg_hw_reset_seq` / `uvm_reg_bit_bash_seq`, register field-value coverage | [Day5/](Day5/) |
| 6 | UVM Packet-Router Verification | Store-and-forward 1→N packet router, input-stream agent + output-backpressure agent, golden per-port FIFO reference-model scoreboard, virtual sequencer + smoke/regress virtual sequences, FIFO-full & backpressure corners, dest×last coverage, SVA (no valid-while-empty, stable-under-backpressure) | [Day6/](Day6/) |
| 7 | UVM Round-Robin Arbiter Verification | Parameterized round-robin arbiter, independent golden pointer-tracking reference model, fairness / anti-starvation checking, backpressure stall + circular skip + idle corners, virtual sequencer + smoke/regress virtual sequences, winner×density coverage, SVA (one-hot grant, grant-is-request, progress, no-grant-when-disabled) | [Day7/](Day7/) |
| 8 | UVM Async FIFO (CDC) Verification | Dual-clock Gray-pointer asynchronous FIFO, 2-flop synchronizer clock-domain crossing, per-domain write/read agents on non-commensurate clocks, golden-queue reference-model scoreboard (order + data integrity across the CDC), fill-to-full & drain-to-empty corners, virtual sequencer + fill/drain & concurrent virtual sequences, data-range coverage, SVA (no overflow, no underflow, single-bit Gray transitions) | [Day8/](Day8/) |
| 9 | UVM ALU Functional-Coverage Verification | Registered 8-op ALU (ADD/SUB/logic/shift/SLT) with {zero,carry,overflow,negative} flags, shared golden reference model driving both scoreboard and coverage, expected-response FIFO scoreboard (result + all four flags), directed/corner/random layered sequences, virtual sequencer + smoke/regress virtual sequences, opcode×flag cross coverage, SVA (zero⇔0, negative=MSB, no-X on response) | [Day9/](Day9/) |
| 10 | UVM AXI4-Stream Skid-Buffer Verification | Full-throughput register slice with one-entry skid register (registered TVALID/TREADY + payload), two active agents (stream source + back-pressure sink) each with driver/monitor/sequencer, golden order-preserving queue scoreboard (lossless, in-order, {TDATA,TKEEP,TLAST} exact, null-byte TKEEP=0), directed showcase + random-packet layered sequences, virtual sequencer + concurrent smoke/regress virtual sequences, tlast×tkeep coverage, AXI-Stream SVA (VALID held until READY, payload stable while stalled, no-X) | [Day10/](Day10/) |
| 11 | UVM SPI Master Verification | Parameterized SPI master (all four CPOL/CPHA modes, programmable clock divider, MSB-first full-duplex), master agent (parallel request driver/monitor) + independent SPI-slave agent (MISO responder driver/monitor), golden full-duplex reference-model scoreboard (master.rx == slave.tx on MISO, slave.rx == master.tx on MOSI), directed all-modes + constrained-random layered sequences, virtual sequencer + concurrent smoke/regress virtual sequences, mode×divider×data coverage, pin-level SVA (cs_n idle, done one-cycle pulse, gated sclk, no-X) | [Day11/](Day11/) |
| 12 | UVM Sequential (Restoring) Divider Verification | Multi-cycle unsigned restoring divider with start/busy/done handshake and divide-by-zero handling, divider agent (handshake driver + independent pin-reconstructing monitor), golden reference-model scoreboard (`/` and `%` plus a structural `q*d+r==dividend`, `r<divisor` identity re-check), directed showcase + corner sequences (0/x, x/1, a<b, max/max, x/0), divide-by-zero stress + constrained-random regression, virtual sequencer + smoke/regress virtual sequences, dividend×divisor / dbz / quotient-zero coverage, handshake SVA (one-cycle done, busy-until-done, bounded completion, no-X) | [Day12/](Day12/) |
| 13 | UVM GPU Tensor-Core Dot-Product (MAC) Engine Verification | Streaming signed multiply-accumulate reduction lane (the GEMM/tensor-core primitive) emitting one dot product per `in_last` with zero-bubble back-to-back vectors, input monitor that reconstructs each vector from the operand stream + output monitor, golden signed dot-product reference-model scoreboard (in-order, `ACC_W` 2's-complement wraparound, extra/missing-result accounting), directed showcase + corners (length-1, all-zero, all-negative, `−128·−128` magnitude stress) + constrained-random variable-length regression, virtual sequencer + smoke/regress virtual sequences, length×sign-cross×zero coverage, result-contract SVA (one-cycle `out_valid` pulse, causality vs `in_valid&in_last`, no-X) | [Day13/](Day13/) |
| 14 | UVM GPU Memory-Coalescing Unit Verification | Warp load/store address coalescer (per-lane byte addresses + active mask → minimum set of cache-line transactions, the coalesced-access / memory-efficiency block), source agent (warp-request driver/monitor) + sink agent (line-stream back-pressure driver + transaction monitor), golden coalescing reference model reused by scoreboard and coverage (unique lines in first-seen lane order, per-line lane mask, `txn_last`, lossless/disjoint lane accounting), directed showcase (8 lanes→2 lines) + corners (fully coalesced, fully scattered, partial mask, all-disabled→0 txns, single lane) + constrained-random regression under random back-pressure, virtual sequencer + smoke/regress virtual sequences, active-lanes×#lines×efficiency coverage, streaming SVA (mask≠0, VALID-held-stable, disjoint lanes, txn_last drains last pending, no-X) | [Day14/](Day14/) |
| 16 | UVM GPU Shared-Memory Bank-Conflict Serializer Verification | Warp shared-memory access serializer (per-lane word addresses → `bank = addr%NBANKS`, one distinct address per bank per phase), **broadcast** (same-bank/same-address lanes coalesced) + **bank-conflict serialization** (same-bank/different-address lanes drained one per phase) + parallel independent banks, request driver/monitor that reassembles `{request, its phase-beat list}`, golden bank-conflict reference model reused by scoreboard (beat-for-beat `ph_served`/`ph_bank_use` + phase-count check) and coverage, directed showcase (3-way bank-0 conflict + broadcast + 4 parallel banks) + corners (conflict-free 8-way, full broadcast, worst-case 8-way serialized, partial mask, single lane, all-inactive empty phase) + constrained-random regression, virtual sequencer + smoke/regress virtual sequences, conflict-degree×active-lanes×broadcast coverage, streaming SVA (ready⇔idle, contiguous stream, monotonic `ph_index`, progress, no-X) | [Day16/](Day16/) |
| 15 | UVM GPU SIMT Reconvergence-Stack Verification | Hardware SIMT divergence/reconvergence stack of `{active-mask, PC}` entries (the warp control-flow block: `INIT` launch, `DIVERGE` split into taken/fall-through with true-divergence push, uniform-taken and uniform-fall-through retarget, `POP` reconverge, `full`/`cmd_ready` overflow guard), single command agent (driver + monitor pairing each accepted command with its resulting TOS status one cycle later), golden **shadow-stack predictor** reference model reused by scoreboard and coverage (checks `tos_mask`/`tos_pc`/`sp`/`empty`/`full` every command), directed launch→diverge→reconverge→retire showcase + corners (uniform branches, nested divergence, single-lane warp, empty-warp launch, pop-past-empty) + constrained-random legal-program regression, virtual sequencer + smoke/regress virtual sequences, op×divergence-outcome×depth coverage, SVA (depth bound, grow-by-2, shrink-by-1, empty⇔sp==0, nonzero TOS mask, no-X) | [Day15/](Day15/) |
| 17 | UVM GPU Warp-Level Bitonic Sorting-Network Verification | Fully-pipelined Batcher bitonic sort of an `N`-record warp (the GPU sort / top-K / order-book-priority primitive: data-independent compare-exchange schedule, `NSTAGE = log2(N)·(log2(N)+1)/2` registered layers, fixed `LAT = NSTAGE+1`, zero-bubble streaming), records are `{key, tag}` compared as one unsigned word → total order → deterministic duplicate-key handling (tag = order-id tie-break), runtime ascending/descending (`in_dir`), driver streams one vector per cycle + monitor FIFO-pairs each input vector with its sorted output (latency-independent), independent **insertion-sort** golden reference reused by scoreboard (lane-by-lane exact match ⇒ sorted permutation) and coverage, directed showcase (reverse-ramp ascending sort) + corners (descending sort, already-sorted identity, equal-keys tie-break, all-identical, min/max extremes, single large element, zero-bubble back-to-back stream) + constrained-random regression with forced-duplicate-key vectors, virtual sequencer + smoke/regress virtual sequences, direction×orderedness×distinct-keys×extremes coverage, SVA (fixed-latency contract, per-lane monotonicity split by direction, no-X) | [Day17/](Day17/) |
| 18 | UVM GPU Warp-Level Parallel Prefix-Sum (Scan) Verification | Fully-pipelined **Kogge-Stone** parallel prefix-sum over an `N`-lane warp (the GPU scan primitive behind stream-compaction / radix-sort digit counts / histogram-CDF / warp-aggregation, and the HFT running-cumulative-volume / order-book-depth ladder & running-P&L block: data-independent add schedule, `L = log2(N)` registered layers `lane[i]+=lane[i-2^(s-1)]`, fixed `LAT = L+2`, zero-bubble streaming), runtime **inclusive/exclusive** scan (`in_excl`; exclusive lane 0 = additive identity), signed **modular `DW`-bit** accumulation with deterministic wraparound, driver streams one vector per cycle + monitor FIFO-pairs each input vector with its scanned output (latency-independent), independent **sequential modular-prefix** golden reference reused by scoreboard (lane-by-lane exact ⇒ correct running sums + mode) and coverage, directed showcase (inclusive ramp 1..8 → triangular 1,3,6,10,15,21,28,36) + corners (exclusive ramp, all-zero, single one, alternating +1/−1, 16-bit wraparound, large-negative, zero-bubble back-to-back stream) + constrained-random regression with small-signed-magnitude vectors, virtual sequencer + smoke/regress virtual sequences, mode×total-sign×zero-content×wrap coverage, SVA (fixed-latency contract, exclusive-lane-0-is-zero, no-X) | [Day18/](Day18/) |
| 20 | UVM Pre-Trade Risk-Check Gate ("Fat-Finger" Order-Validation Gateway) Verification | Fully-pipelined, fixed-latency (`LAT=PIPE+1`) pre-trade risk gate — the first, most latency-critical block in an FPGA trading gateway: validates one order `{side,price,qty}` per cycle (zero-bubble) against runtime-programmed limits and emits accept/reject + a **strict-priority reason code** (1 qty-zero, 2 qty-max, 3 price-band/collar, 4 max-notional `price*qty`, 5 net-position limit), maintaining a **stateful signed net-position** accumulator that advances only on accepts and enforces a symmetric `±pos_limit`; UVM driver programs the limits then streams orders, latency-independent FIFO-pairing monitor, **independent stateful golden reference model** reused by the scoreboard (checks accept+reason+echoed side+running position in arrival order), directed showcase (one order per reject reason + accepts + a position-reducing sell) + boundary corners (values exactly at every limit accepted, strict `+1` breaches rejected on both position limits, a full swing `+pos_limit`→0→`-pos_limit`) + constrained-random regression squeezed toward the limits, virtual sequencer + smoke/regress virtual sequences, reason×side coverage, SVA (fixed-latency contract, accept⇔reason==0, reason-in-range, no-X) | [Day20/](Day20/) |
| 21 | UVM Market-Data Sequence Gap Detector & Duplicate Suppressor Verification | Cut-through, fixed-latency (`LAT=PIPE+1`) **feed-handler front end** — the first, most latency-critical block in an FPGA market-data / ticker-plant ingest path: sanitizes a monotonically-numbered message stream (exchanges publish it on redundant A/B lines) so downstream sees each sequence **exactly once, in order**. Against a running **next-expected** counter it classifies every message `PASS` (in-order → forward, advance), `DUP` (seq < expected → duplicate B-line copy or stale retransmit → drop, freeze), or `GAP` (seq > expected → report `gap = seq−expected` missing-message count for the retransmit request, forward, and **resync** `expected := seq+1`); a detector/sanitizer, not a reorder buffer, so it never stalls the line-rate stream. UVM driver programs the session start (`cfg_init_seq`) then streams one message/cycle zero-bubble, latency-independent FIFO-pairing monitor, **independent stateful golden reference model** reused by the scoreboard (checks action + forward flag + gap size + echoed seq + running next-expected in arrival order), directed showcase (PASS / duplicate-copy / gap=2 / stale-retransmit in one zero-bubble stream) + corners (resume, immediate duplicate, minimal gap-of-1, large gap, far-behind stale, resync-and-continue) + constrained-random regression that random-walks around the expected sequence, virtual sequencer + smoke/regress virtual sequences, action×gap-size coverage, SVA (fixed-latency contract, action-in-range, `fwd ⇔ action≠DUP`, `gap≠0 ⇔ GAP`, no-X) | [Day21/](Day21/) |
| 19 | UVM Best-Bid/Best-Offer (BBO) Top-of-Book Reduction-Tree Verification | Fully-pipelined **argmax + argmin reduction tree** over an `N`-level price ladder — the single most latency-critical block in an FPGA matching engine / market-data handler (top-of-book: every cycle emit **best bid** = highest price + its level and **best offer** = lowest price + its level; the generic hardware winner-take-all / priority-select / top-1 primitive): balanced tree collapses `N` candidates to 1 in `L = log2(N)` registered compare layers, fixed `LAT = L+2`, zero-bubble one-snapshot-per-cycle streaming, deterministic **lowest-index-wins tie-break** (left child always lower index ⇒ keep-left-on-tie is globally lowest-index), per-level **populated-mask** so empty levels never win + empty-book identity outputs (`any=0`, max=0, min=all-ones), driver streams one ladder per cycle + monitor FIFO-pairs each ladder with its BBO result (latency-independent), independent **lowest-index argmax/argmin** golden reference reused by scoreboard (value+index+any exact match) and coverage, directed showcase (full book 110@lvl3 tie→lvl3, offer 100@lvl0) + corners (single level, empty book, simultaneous max+min ties, all-equal, price extremes 0x0000/0xFFFF, sparse mask, zero-bubble back-to-back stream) + constrained-random regression with tie-forcing small-range prices, virtual sequencer + smoke/regress virtual sequences, occupancy×max-tie×min-tie×edge coverage, SVA (fixed-latency contract, best-bid≥best-offer, index-in-range, no-X) | [Day19/](Day19/) |
| 23 | UVM Streaming CRC-32 (Ethernet FCS) Generator/Checker Verification | Byte-serial one-byte-per-cycle **reflected CRC-32 / ISO-HDLC** engine (the zlib / PNG / IEEE-802.3 Ethernet Frame-Check-Sequence block, poly `0xEDB88320`, init/xorout `0xFFFFFFFF`) — **bit-identical to `zlib`/`binascii.crc32`** so an independent golden model checks it byte-for-byte (canonical `CRC32("123456789")==0xCBF43926`), `sop`/`eop`-framed zero-bubble stream with a per-frame mode latched at `sop`: **GENERATE** (emit the FCS a transmitter appends) + **CHECK** (receiver residue verdict — a good frame's `message‖FCS` folds to the constant residue `0x2144DF1C` ⇒ `out_ok=1`, a corrupted frame ⇒ `out_ok=0`), fixed latency `LAT=PIPE` from `in_eop`, full UVM env (byte-stream driver + frame-reconstructing monitor, FIFO golden-CRC scoreboard checking `{crc,mode,ok}` in arrival order, mode×length-class + mode×ok coverage, smoke/regress virtual sequences), directed showcase (`"123456789"` FCS + good/corrupt check frame) + corners (single-byte, all-zero, all-`0xFF`, back-to-back zero-bubble) + constrained-random mixed-mode regression with single-bit-corrupted check frames, SVA (fixed-latency contract, result-caused-by-`eop`, generate-mode `ok`, `sop`/`eop`⇒`valid`, no-X) | [Day23/](Day23/) |
| 22 | UVM Token-Bucket Order-Rate Limiter ("Exchange Gateway Throttle") Verification | Cut-through, fixed-latency (`LAT=PIPE+1`) **token-bucket rate limiter** — the outbound self-policing throttle in an FPGA trading gateway that keeps a firm under the exchange's orders-per-second ceiling before a message leaves the wire: a **lazy-refill** bucket where each request carries a monotonically non-decreasing **timestamp** `ts` so refill = `(ts−last_ts)·REFILL_PER_TICK` saturated at `BUCKET_MAX` is realized on access (no per-cycle adder) — making admission a **pure function of the `{ts,cost}` stream**, latency-independent and transaction-checkable; against the refilled level it emits a **strict-priority reason code** (0 GRANT spend `cost`, 1 THROTTLE rate-exceeded, 2 ZEROCOST malformed, 3 OVERSIZED `cost>BUCKET_MAX` unsatisfiable) + grant flag while advancing the running `{tokens,last_ts}` on every accepted request; UVM driver programs the session (`cfg_init_ts`, bucket→full) then streams one request/cycle zero-bubble, latency-independent FIFO-pairing monitor, **independent stateful golden token-bucket reference model** reused by scoreboard (grant+reason+echoed ts/cost+available+remaining tokens in arrival order) and coverage, directed showcase (burst-drain→throttle→refill-recover→zerocost→oversized in one zero-bubble stream) + corners (long-idle saturation, exact-boundary grant, one-over throttle, single-shot full drain, oversized, zerocost) + constrained-random regression with same-tick bursts and limit-squeezed costs, virtual sequencer + smoke/regress virtual sequences, reason×cost-class + avail-level coverage, SVA (fixed-latency contract, reason-in-range, `grant⇔reason==GRANT`, `tokens≤BUCKET_MAX`, grant-implies-could-pay, monotonic timestamps, no-X) | [Day22/](Day22/) |
| 24 | UVM SECDED (72,64) Extended-Hamming ECC Encoder/Decoder Verification | Parameterized **Single-Error-Correct / Double-Error-Detect** extended-Hamming ECC engine — the integrity guard **inside** memory (every ECC DRAM DIMM, cache line, register file, on-die SRAM): where a CRC only *detects* wire corruption, an ECC code *corrects* it in place. Showcased at the classic **(72,64)** geometry (64 data + 8 check bits): `HAM = smallest r with 2^r ≥ DW+r+1` Hamming parity bits at power-of-two positions + one **overall parity** bit lifting plain Hamming (SEC) to SECDED, textbook construction so an independent model is **bit-exact** (verified in Python over 200k words × {0,1,2} flips before RTL). Per-op `ENCODE` (data→codeword) / `DECODE` (received word→corrected data + raw syndrome + `sbe`/`dbe` verdict) at fixed latency `LAT=PIPE`, zero-bubble; decode classifies on `{P,S}` = {overall-parity, syndrome}: `0/0` clean, `1/·` single-bit **corrected** (`sbe`), `0/≠0` double-bit **detected** uncorrectable (`dbe`). Full UVM env — driver that golden-encodes then **injects `nflip` faults**, request/result-pairing monitor, FIFO golden-model scoreboard checking `{op,code,data,syndrome,sbe,dbe}` bit-for-bit in arrival order (model mirrors the DUT's exact correction so even the double-bit "correction" matches), op×error-class{none,single,double} + syndrome-zero coverage, smoke/regress virtual sequences — plus directed showcase (encode → clean/1-bit/2-bit decode) + corners (all-zero, all-ones, Hamming-parity-bit / overall-parity-bit / data-bit flips, back-to-back zero-bubble) + constrained-random 0/1/2-flip fault campaign, SVA (fixed-latency contract, result-caused-by-request, `sbe`/`dbe` mutually exclusive, ENCODE-never-errors, no-X) | [Day24/](Day24/) |
| 25 | UVM Rotation-Mode CORDIC (Hardware sin/cos & Vector Rotator) Verification | Fully-pipelined **rotation-mode CORDIC** — the multiplier-free shift-and-add vector rotator behind every hardware **sin/cos** generator, polar⇄rectangular converter, DDS/QAM/SDR **phase rotator**, and FFT **twiddle**-multiply: `NITER=16` registered micro-rotations by `±atan(2^-i)` drive the angle accumulator to zero so `(x,y)` ends up rotated by the requested angle and scaled by the constant CORDIC gain `K≈1.64676` (`x_out≈K·(x·cos−y·sin)`, `y_out≈K·(x·sin+y·cos)`; preload `x=round(2^13/K)=4975,y=0` to read cos/sin directly), fixed latency `LAT=NITER+1=17`, zero-bubble; a combinational **quadrant-fold** pre-stage maps any angle in `[-π,π]` into `[-π/2,π/2]` (native convergence is only `|z|≤1.7433 rad`) by rotating an extra `±π` and negating the vector, giving a full-circle rotator; signed Q2.13 fixed-point with `XYW=DW+4` guard-bit datapath. Full UVM env — one-request/cycle driver + latency-independent FIFO-pairing monitor, **independent real-valued golden model** (`$cos`/`$sin`·`K`, reused by scoreboard and coverage) checking `{out_x,out_y}` within a small fixed-point tolerance (`TOL=24` LSB; measured worst-case ≈11 LSB over 200k rotations) + echoed-angle, quadrant×fold×vector-kind coverage, smoke/regress virtual sequences — plus directed sin/cos sweep across all quadrants + fold (the captured waveform) + corners (identity, `±π`, axis `+90°` rotations, general vectors into the fold) + constrained-random full-range campaign (4018 checks PASS on Icarus), SVA (fixed-latency contract, no-X) | [Day25/](Day25/) |
| 27 | UVM Self-Synchronizing Scrambler/Descrambler (10GBASE-R PCS) Verification | Parameterized WIDTH-bit-parallel **multiplicative (self-synchronizing) scrambler/descrambler** — the SerDes physical-layer data-whitening block (IEEE 802.3 **10GBASE-R PCS**, generator `G(x)=1+x^39+x^58`, 58-bit state) that guarantees clock-recovery transitions and spreads the line spectrum; the SerDes companion to the link-layer CRC (Day 23) / ECC (Day 24) and the serial buses UART/SPI/I²C. One RTL module covers both directions via `MODE_DESCRAMBLE` (only the bit fed into the register differs), with the WIDTH serial steps unrolled in `always_comb` (the classic parallelize-the-LFSR exercise). The headline verified property is **self-synchronization**: the descrambler is deliberately reset to a *different* seed than the scrambler yet re-derives the transmitter state from the received bits alone and recovers the payload exactly from word `ceil(58/8)=8` (58 received bits) on. Full UVM env — one-word/cycle zero-bubble driver + input monitor + midpoint/endpoint output monitor, **independent bit-serial golden reference model** (a different implementation from the parallel RTL, pinned to 28 **Python Known-Answer vectors**) reused by scoreboard (scrambled bit-exact + descrambled bit-exact + self-sync recovery == original once locked) and coverage, data-class×wire-error + lock-state coverage, smoke/regress virtual sequences — plus directed showcase (self-sync recovery from a wrong seed = the captured waveform, all-zero whitening) + corners (valid-gap hold, **single-bit wire error → bounded 0/39/58 error burst → automatic re-lock**, all-ones burst) + constrained-random regression (**~4000 words, 0 errors on Icarus**), SVA (per-stage fixed-latency `out_valid`==prev `in_valid`, no-X) | [Day27/](Day27/) |
| 26 | UVM I²C Master Controller Verification | Single-master **open-drain I²C** controller (the two-wire SDA/SCL bus behind every board-level sensor / EEPROM / RTC / codec / PMIC) — the classic serial link that completes UART (Day 4) and SPI (Day 11), and whose distinctive verification challenge is the **bidirectional wired-AND open-drain bus**: a device never drives a hard ‘1’, only pulls low or releases to a pull-up, SDA must be **stable while SCL is high** and change only while SCL is low, and START/STOP are defined by the illegal-looking SDA-transition-while-SCL-high. DUT does one **single-byte** 7-bit-addressed transaction per `start`: `WRITE` = START·{addr,W}·[ACK]·data·[ACK]·STOP, `READ` = START·{addr,R}·[ACK]·data(slave)·masterNACK·STOP, raising `ack_error` when the address is not ACKed; programmable bit rate (`DIV`, SCL period `4·DIV` clks) via a four-quarter-per-bit scheme so SDA only moves in the SCL-low quarter. Full UVM env — transaction-level driver (programs the slave read byte, pulses `start`, waits `done`) + monitor (latches the request at launch, publishes `{ack_error,rd_data,slave-captured-write-byte}` at `done`), **independent transaction-level golden reference model** reused by scoreboard (ack_error + read byte == slave memory + written byte == slave capture, in arrival order) and coverage, a behavioral **open-drain I²C slave model** (START/STOP detect, address-match ACK/NACK, write capture, MSB-first read playback) as the bus far end, `rw×ack` cross + data-class {0x00,0xFF,mid} coverage, smoke/regress virtual sequences — plus directed showcase (write, read, address-NACK on both R/W, data corners) + constrained-random regression (**208 checks PASS on Icarus**), SVA (`done` one-cycle pulse, `done`-inside-`busy`, no-X on bus while busy) | [Day26/](Day26/) |
| 28 | UVM IEEE-754 binary32 Floating-Point Adder/Subtractor Verification | Fully-pipelined **IEEE-754 binary32 (single-precision) floating-point adder/subtractor** — the block inside every FPU, GPU shader core, DSP MAC and neural-net accumulator, and the canonical example of a design whose happy path is trivial while its corner space is enormous: **alignment** (the smaller operand shifted right by the exponent difference, with everything past the end OR-ed into a **sticky** bit so a 2^-100 term still nudges the rounding of a 2^100 one), **cancellation** (`a-b` with `a≈b` destroying up to 24 significand bits and needing a full-width leading-zero normalise), **round-to-nearest-EVEN** on `{L,G,sticky}` (exact ties rounding toward the even significand, a round-up carrying straight out of the significand into `exponent++`), **subnormals** (no hidden bit + fixed effective exponent on input; normalisation clamped at the exponent floor on output; subnormal→normal round-up and normal→subnormal cancellation), and **specials** (signed zeros with `(-0)+(-0)=-0` but `(+0)+(-0)=+0`, `x+(-x)=+0`, infinity arithmetic, `(+inf)+(-inf)`→invalid, NaN precedence over inf, overflow→inf, canonical qNaN `0x7FC00000` so the result is a pure function of the operands) — three register stages, fixed latency `LAT=3`, one result per cycle zero-bubble, plus the four exception flags `{inv,ovf,unf,inx}`. The methodological centre of gravity is **"what checks the checker?"**: a rounder's reference model is as easy to get subtly wrong as the RTL, so the golden model is **pinned to the host CPU's own IEEE-754 hardware** — `docs/gen_kat.py` cross-checks a Python model against **numpy float32** bit-exactly over 400k+ randomised ops plus an exhaustive small-exponent sweep and emits a 48-row **Known-Answer Table** of numpy's answers; `kat_selfcheck()` replays it through the SystemVerilog model *before* any DUT result is judged; the KAT is then replayed **through the RTL**; and the RTL was separately driven against **200 000 numpy-derived vectors (result + all four flags): 0 mismatches**. Full UVM env — one-op/cycle driver + latency-independent monitor publishing requests and results on separate ports, FIFO-pairing reference-model scoreboard (word + all four flags, arrival order, readable float-form mismatch reports), coverage collector downstream of the scoreboard so operand class can cross **result** class, virtual sequencer + smoke/regress virtual sequences — plus directed showcase (the captured waveform) + KAT replay + a 16x16x{add,sub}=512 **landmark cross-product** + a half-ULP **tie campaign** (alternating even/odd retained LSB, plus quarter- and three-quarter-ULP neighbours) + the subnormal exponent floor + the overflow boundary + `kind`-shaped constrained-random regression (**5958 ops, 0 mismatches on Icarus**, 382 subnormal and 172 infinite results), operand-class×result-class / alignment-distance / flag coverage with four crosses, SVA (fixed-latency contract, causality, canonical-NaN-only, ovf⇒inx∧inf, inv⇒qNaN, no-X) — and one **proven invariant asserted rather than assumed**: since the exact sum of two binary32 values is always a multiple of 2^-149, addition can never round inside the subnormal range, so **underflow is unreachable** (`out_unf` must always be 0 — true for add/sub, NOT for multiply/divide) | [Day28/](Day28/) |
| 29 | UVM 8b/10b Line-Code Encoder/Decoder Verification | The SerDes physical-layer **line code** behind Gigabit Ethernet (802.3 cl.36), PCIe gen1/2, SATA, DisplayPort and Fibre Channel — the layer *underneath* the CRC (Day 23), ECC (Day 24) and scrambler (Day 27): it maps each byte to a 10-bit codeword so the wire gets **DC balance** (4/5/6 ones per word, running disparity pinned to ±1 forever, so an AC-coupled link can pass it), **transition density** (no run > 5 bits, so the CDR never starves) and **framing** (12 control **K** symbols outside the 256 data codewords, three carrying the **comma** `0011111`/`1100000` that by construction only occurs symbol-aligned — how a receiver finds symbol boundaries in a raw bit stream). Encoder = 5b/6b + 3b/4b sub-block tables with RD threaded through both halves in sequence, plus the three exceptions that carry all the subtlety: the **balanced-but-still-alternating** entries `D.07` and `D.x.3`, the alternate **`D.x.A7`** 3b/4b encoding at six x values, and control sub-blocks that alternate even when balanced. Receiver = **decode-then-verify-by-re-encode** (reverse sub-block tables recover a candidate in O(1), then the candidate is pushed back through the encoder's own rules and compared with what arrived) making it strict — it rejects sub-block pairs no transmitter can emit — and classifying every possible word as clean / **disparity error** (a codeword, wrong polarity for RD) / **code error** (not a codeword), with RD advanced from the bits that *arrived* so corruption costs a transient, self-correcting RD disagreement. The methodology centre of gravity is **"a table code is wrong in ways that still work"** — a transposed bit gives RTL whose encoder and decoder share the typo, round-trip all 256 bytes, and violate the line code on the wire — so the tables are **proved, not transcribed**: `docs/gen_kat.py` checks the five defining properties exhaustively (DC balance, bounded RD, **run ≤ 5 across all ~143 000 legal codeword junctions**, unique decodability, comma uniqueness/alignment/non-manufacture) and caught 2714 violations from exactly the two balanced-alternating entries on its first run; it also **derives** the standard's `K.28.7` restriction (the 64 successors that would break run ≤ 5 or forge a comma) rather than assuming it. The golden model then contains **no rules at all** — a flat 1024-entry encode ROM plus a 2048-entry decode table built by brute-force inversion — so a DUT rule bug cannot hide in it, and `property_selfcheck()` + a 48-vector KAT re-prove the model inside the simulator before any DUT result is judged. Full UVM env — one-symbol/cycle zero-bubble driver, request/transmit/receive monitors, dual-queue reference-model scoreboard (codeword, wire word, both RDs, kerr, comma, recovered symbol, both error verdicts), coverage **downstream of the scoreboard** so symbol class crosses result verdict, smoke/regress virtual sequences — plus directed showcase (the captured waveform: comma framing, a caught bit error, and an unencodable request whose all-zero word knocks the receiver's RD out of step, surfacing one symbol later then self-correcting) + all 256 data bytes + all 12 K symbols from both RD states + the alternate-encoding corners + a bit-flip walk across all ten wire positions + a **single-bit error detection-latency sweep** that measures rather than assumes (60 isolated flips: 39 flagged immediately, 21 flagged exactly 5 symbols later when the first RD-sensitive symbol arrives, **0 never flagged**) + pseudo-random regression (**4944 symbols, 0 mismatches on Icarus**), SVA (fixed-latency contract, DC balance, run-length across junctions with the derived K.28.7 exemption, comma provenance, error-class exclusivity, RD stability, no-X) | [Day29/](Day29/) |
